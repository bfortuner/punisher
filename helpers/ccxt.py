
def init_exchange(exchange_id, exchange_cfg):
    # TODO: lookup exchanges, call ccxt with cfg for that exchange
    # ex. ccxt.gemini(exchange_cfg)
    # OR find some other way to do this crap
    pass